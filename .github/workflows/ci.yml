name: Helm Chart CI

on:
  pull_request:
    types: [opened, synchronize, reopened]
    branches: [ main ]
  push:
    branches: [ main ]

jobs:
  helm-lint-and-template:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write

    steps:
    - name: Checkout
      uses: actions/checkout@v3

    - name: Set up Helm
      uses: azure/setup-helm@v4.2.0
      with:
        version: 'latest'

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.12'
        check-latest: true

    - name: Generate Helm Template and Write to File
      run: helm template . > helm_output.yaml

    - name: Upload Helm Template as Artifact
      uses: actions/upload-artifact@v2
      with:
        name: helm-template-output
        path: helm_output.yaml

    - name: Download Helm Template Artifact
      uses: actions/download-artifact@v2
      with:
        name: helm-template-output

    - name: Set Helm Output as Env Var
      run: |
        echo "helm_output<<EOF" >> $GITHUB_ENV
        cat helm_output.yaml >> $GITHUB_ENV
        echo "EOF" >> $GITHUB_ENV
        echo "::set-output name=helm_output::$(cat helm_output.yaml)"

    - name: Create or Update Comment with Helm Template
      uses: peter-evans/create-or-update-comment@v4
      with:
        issue-number: ${{ github.event.pull_request.number }}
        body: |
          **Helm Template Output:**
          ```
          ${{ steps.helm-output.outputs.helm_output }}
          ```
        reactions: 'eyes'

    - name: Pre-commit Checks
      uses: pre-commit/action@v2.0.3
